// 배열
// 1. 배열 생성

let arrA = new Array(); // 배열 생성자.
let arrB = []; // 배열 리터럴방식 (주로 많이 사용)
let arrC = [1, 2, 3, true, null, undefined, () => {}, {}, []];

// 배열 요소 접근.
let item1 = arrA[0];
let item2 = arrA[1];
arrC[3] = "hello"; // 변경도 가능.
